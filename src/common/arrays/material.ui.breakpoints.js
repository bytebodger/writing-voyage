export const materialUiBreakpoints = Object.freeze([
   Object.freeze({
      name: 'xs',
      minWidth: 0,
      maxWidth: 599,
   }),
   Object.freeze({
      name: 'sm',
      minWidth: 600,
      maxWidth: 959,
   }),
   Object.freeze({
      name: 'md',
      minWidth: 960,
      maxWidth: 1279,
   }),
   Object.freeze({
      name: 'lg',
      minWidth: 1280,
      maxWidth: 1919,
   }),
   Object.freeze({
      name: 'xl',
      minWidth: 1920,
      maxWidth: Number.MAX_SAFE_INTEGER,
   }),
]);
